A maze solver using (what i think is) A* algorithm i made for learning c#. It can solve any image inputs as long as the paths are near white color. Keep in mind higher quality images will take longer to solve so i recommend downscaling the image. (The program has a built-in scaler)
